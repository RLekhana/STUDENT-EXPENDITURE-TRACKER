<!DOCTYPE html>
<html>
<head>
    <title>Dashboard</title>
</head>
<body>
    <h2>Welcome, {{ session['username'] }}</h2>
    <p>Total Expenses: ₹{{ total }}</p>
    <a href="{{ url_for('add_expense') }}">Add New Expense</a>
    <a href="{{ url_for('logout') }}">Logout</a>
    <hr>
    <h3>Your Expenses:</h3>
    <table border="1">
        <tr>
            <th>Amount</th><th>Category</th><th>Description</th><th>Date</th>
        </tr>
        {% for e in expenses %}
        <tr>
            <td>₹{{ e.amount }}</td>
            <td>{{ e.category }}</td>
            <td>{{ e.description }}</td>
            <td>{{ e.date.strftime('%Y-%m-%d') }}</td>
        </tr>
        {% endfor %}
    </table>
</body>
</html>
